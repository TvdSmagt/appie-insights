[pytest]
testpaths = tests
markers =
    slow: tests that load the sentence-transformers model (deselect with -m "not slow")
    integration: tests that require live AH credentials and make real API calls
addopts = -v --tb=short
